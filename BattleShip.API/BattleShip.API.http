# Essais manuels : utiliser api.http à la racine du dépôt (port 8080, docker compose).
# Ce fichier est conservé pour compatibilité IDE ; il n'est plus la référence des scénarios.

@api = http://localhost:8080

### Voir api.http à la racine pour les scénarios complets
GET {{api}}/openapi/v1.json
